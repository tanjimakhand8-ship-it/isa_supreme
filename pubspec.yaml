name: isa_supreme
description: ISA SUPREME + Jarvis Prime Final
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  mime: ^1.0.5
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  path_provider: ^2.1.3
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
