name: isa_supreme
description: ISA SUPREME + Jarvis Prime
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  mime: ^1.0.5
  path_provider: ^2.1.3
  open_filex: ^4.4.0
  google_fonts: ^6.1.0
  flutter_phone_direct_caller: ^2.1.0
  permission_handler: ^11.3.1
  flutter_tts: ^3.8.5
  speech_to_text: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
